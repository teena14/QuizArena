# screenshots

Place UI screenshots here. The README references `hero.png` at the top.

Suggested captures:
- `hero.png`         — landing page or student quiz view (used in README hero)
- `teacher-dash.png` — teacher dashboard with quiz list
- `quiz-builder.png` — quiz editor with questions
- `take-quiz.png`    — student taking a timed quiz
- `results.png`      — results breakdown page
- `leaderboard.png`  — per-quiz leaderboard
