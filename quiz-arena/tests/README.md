# tests

Unit and end-to-end tests live here.

## Planned Structure

```
tests/
├── unit/          # Jest or Vitest unit tests (lib utilities, validators)
└── e2e/           # Playwright end-to-end tests (auth flows, quiz taking)
```

See the [Roadmap](../README.md#roadmap) for status.
